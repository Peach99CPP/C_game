#include <bits/stdc++.h>
using namespace std;
//Ñ°ÕÒÒò×Ó

int main()
{
    char ch;
    cin >> ch;
    printf("  %c  \n", ch);
    printf(" %c%c%c \n", ch, ch, ch);
    for (int i = 0; i < 5;++i)
    {
        printf("%c", ch);
    }

        // double temp;
        // cin >> temp;
        // printf("%f\n%.5f\n%e\n%g", temp,temp,temp, temp);

        // char ch;
        // int num;
        // float f1;
        // double f2;
        // cin >> ch >> num >> f1 >> f2;
        // printf("%c %4d %.2f %.12f", ch, num, f1, f2);

        // double x;
        // cin >> x;
        // printf("%.12lf", x);

        // float x;
        // cin >> x;
        // printf("%.3f", x);
        return 0;
}